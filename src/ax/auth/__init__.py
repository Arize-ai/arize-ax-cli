"""OAuth/PKCE authentication primitives for the ax CLI."""

# OAuth client identifier the CLI registers as with the Arize OAuth app.
# Not user-configurable — pinned to the CLI's registration.
OAUTH_CLIENT_ID = "arize-cli"
